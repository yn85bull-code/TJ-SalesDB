# TJ-SalesDB Workspace

This repository stores the TJ-SalesDB Apps Script/HTML dashboard files,
reference mapping workbooks, and local helper scripts used during development.

Main production files:

- `outputs/tj-sales-os-reference-dashboard-Code.gs`
- `outputs/tj-sales-os-reference-dashboard.html`

Reference mapping files are under:

- `outputs/reference-mapping/`

Local scratch and helper scripts are under:

- `work/`
- `tools/`
